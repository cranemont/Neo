{
  "name": "neo-monorepo",
  "version": "1.0.0",
  "description": "Neo Monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "biome format --write ./",
    "lint": "biome check --write ./",
    "build": "pnpm -r build",
    "start:explorer": "pnpm build && pnpm --filter explorer start",
    "start:client": "pnpm build && pnpm --filter client-electron start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.53.0-alpha-2025-06-01"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
